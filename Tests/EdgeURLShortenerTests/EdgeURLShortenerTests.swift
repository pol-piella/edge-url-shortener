import XCTest
@testable import EdgeURLShortener

final class EdgeURLShortenerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(EdgeURLShortener().text, "Hello, World!")
    }
}
